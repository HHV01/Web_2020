@model IEnumerable<MyPhamOnline.Models.DANHSACHSP>
<ul class="list-group">
    <li class="list-group-item active" style="font-weight:bold; text-align:center">Danh Mục Sản Phẩm</li>
    @foreach (var item in Model)
    {
    <a href="@Url.Action("DanhSachSanPham","MyPhamOnline", new { iMaDS= item.MaDS })"
       class="list-group-item">@item.TenDS</a>

    }

</ul>